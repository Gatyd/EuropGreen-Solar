export default defineAppConfig({
  ui: {
    colors: {
      primary: 'green', // Utilise notre vert personnalisé redéfini
      secondary: 'blue', // Utilise notre bleu du drapeau français
      neutral: 'slate'
    }
  }
})
